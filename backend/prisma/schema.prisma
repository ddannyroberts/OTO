generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model RevenueDaily {
  id       Int    @id @default(autoincrement())
  date     String @unique
  guests   Int
  revenue  Int
  discount Int
}